<?php if (isset($user)) : ?>
    <?php foreach ($user as $key => $value) : ?>
        <div>
            <?= htmlspecialchars($key) ?>: <?= htmlspecialchars($value) ?>
        </div>
    <?php endforeach; ?>
<?php endif; ?>
<a href="/users"><h1>🔙</h1></a>

